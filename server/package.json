{
    "name": "server",
    "version": "0.0.1-alpha.1",
    "description": "A simple server built with fastify, graphql and arangodb.",
    "main": "lib/server.js",
    "author": "Jens Dobberthin",
    "license": "MIT",
    "scripts": {
        "server:dev:watch": "nodemon -r esm -e js,gql,json -w ../server -w ../packages -w ../services -w ../config lib/server.js"
    },
    "dependencies": {
        "esm": "^3.2.25",
        "fastify-blipp": "^2.3.1",
        "fastify-cors": "^3.0.3",
        "fastify-url-data": "^2.4.0",
        "pino-pretty": "^4.0.0"
    },
    "devDependencies": {
        "nodemon": "^2.0.3"
    },
    "private": true
}
